//No tiene estado, es decir, no depende de un estado, ni de un ciclo de vida
//solo va a contener la logica del la app
//trabajan con la parte funcional (con funciones)
import React from 'react';

const Stateless = () => {  //No tiene estado ni cliclo de vida
    return (  // solo tiene la parte funciona
        <h1>Hola Mundo</h1>
    );
};

export default Stateless;
