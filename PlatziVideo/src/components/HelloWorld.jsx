import React from 'react';
//Con solo importar el archivo archivo SaSS (App.scss), ya estamos utilizando nuestros estilos
import '../assets/styles/App.scss';

const HelloWorld = () => (  //si usamos llaves no es un return implicito y hay que indicarlo, si usamos paarentesis es un return implicito OJO
    <h1>Hola Mundo</h1>
)

export default HelloWorld;
